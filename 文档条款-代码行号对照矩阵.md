# 架构开发文档_opt 与当前代码实现逐条对照矩阵

- 文档版本: `架构开发文档_opt.md`
- 代码基线: 当前工作区 `agent_framework/` + `tests/test_integration.py`
- 审查日期: 2026-03-13

## 总结结论

- 结论: **绝大多数条款已实现**。
- 唯一明确差异: **16.2 子 Agent 派生流中的 `build spawn seed` 仍是部分实现（方法存在但主流程未接线）**。
- 其余关键修复项（权限优先级、Skill override 边界、A2A 接线、子 Agent 安全边界、记忆治理接口）均可在代码与回归测试中找到对应。

---

## A. 文档“缺陷修订清单”(5章) 对照

| 文档条款 | 文档行号 | 代码实现 | 状态 | 备注 |
|---|---:|---|---|---|
| 缺陷1: `BaseAgent` 不再承载依赖容器 | `架构开发文档_opt.md:169-172` | `agent_framework/agent/runtime_deps.py`（依赖集中承载）；`agent_framework/agent/coordinator.py:56-61`（`run(agent, deps, task)`） | 已实现 | 运行依赖由 `AgentRuntimeDeps` 注入 |
| 缺陷2: Protocol/Base/Default 分层 | `:173-176` | `agent_framework/protocols/core.py:20-199`；`agent_framework/memory/base_manager.py:17-182`；`agent_framework/memory/default_manager.py:39-204` | 已实现 | 契约/骨架/默认实现三层分离 |
| 缺陷3: store 不做业务去重合并 | `:177-180` | `agent_framework/memory/default_manager.py:166-197`（去重/合并）+ `agent_framework/memory/sqlite_store.py:78-189`（仅CRUD） | 已实现 | 业务逻辑在 manager，store 仅存取 |
| 缺陷4: 记忆层不返回 `formatted_block` | `:181-184` | `agent_framework/context/source_provider.py:38-55`（格式化）+ `agent_framework/memory/base_manager.py:48-50`（返回 `list[MemoryRecord]`） | 已实现 | 格式化职责在 Context 层 |
| 缺陷5: SessionState 唯一持有会话历史 | `:185-188` | `agent_framework/models/session.py:10-30`；`agent_framework/agent/coordinator.py:64,247-265` | 已实现 | 会话消息写入/读取集中在 `SessionState` |
| 缺陷6: 权限机制优先级统一 | `:189-192` | `agent_framework/agent/coordinator.py:212-220`（schema ceiling）；`agent_framework/agent/loop.py:157-171`（runtime gate + hook） | 已实现 | 顺序为 Capability -> ScopedRegistry -> hook |
| 缺陷7: Skill override 生效边界 | `:193-196` | `agent_framework/agent/coordinator.py:150-175`（`effective config`） | 已实现 | 不直接修改 `agent_config` |
| 缺陷8: MemoryScope 插件语义落地 | `:197-200` | `agent_framework/subagent/memory_scope.py:58-121`；`agent_framework/subagent/factory.py:135-151` | 已实现 | 父快照只读；共享写回经父 manager |
| 缺陷9: spawn 语义从普通工具抽象 | `:201-204` | `agent_framework/protocols/core.py:73-85`（`DelegationExecutorProtocol`）；`agent_framework/tools/executor.py:183-203` | 已实现 | `subagent`/`a2a` 走委派执行 |
| 缺陷10: 用户记忆治理边界 | `:205-207` | `agent_framework/entry.py:231-272`；`agent_framework/memory/base_manager.py:136-175` | 已实现 | 列表/删除/钉住/启停/清空/总开关 |

---

## B. “实现约束总结”(21章) 逐条矩阵

| # | 文档条款（21章） | 文档行号 | 代码行号 | 状态 |
|---:|---|---:|---|---|
| 1 | `RunCoordinator` 管 run 生命周期；`AgentLoop` 仅 iteration | `架构开发文档_opt.md:1689` | `agent_framework/agent/coordinator.py:31-140`; `agent_framework/agent/loop.py:28-103` | 已实现 |
| 2 | `BaseAgent` 管策略；依赖由 `AgentRuntimeDeps` 承载 | `:1690` | `agent_framework/agent/coordinator.py:56-61`; `agent_framework/agent/runtime_deps.py` | 已实现 |
| 3 | 记忆层默认仅 Saved Memory，不做全文检索 | `:1691` | `agent_framework/memory/default_manager.py:39-46`; `agent_framework/memory/sqlite_store.py:38-189` | 已实现 |
| 4 | 默认记忆不引入 `rank_bm25` | `:1692` | `pyproject.toml`（无 `rank_bm25`）；`agent_framework/memory/default_manager.py:57-97` | 已实现 |
| 5 | `BaseMemoryManager` + `MemoryStoreProtocol` 为主干 | `:1693` | `agent_framework/memory/base_manager.py:17-182`; `agent_framework/protocols/core.py:91-137` | 已实现 |
| 6 | 去重/合并仅在 MemoryManager，不进 store | `:1694` | `agent_framework/memory/default_manager.py:166-197`; `agent_framework/memory/sqlite_store.py:78-189` | 已实现 |
| 7 | 记忆层不做 prompt 格式化 | `:1695` | `agent_framework/context/source_provider.py:38-55`; `agent_framework/memory/base_manager.py:27-31` | 已实现 |
| 8 | Session History 由 `SessionState` 唯一持有 | `:1696` | `agent_framework/models/session.py:10-30`; `agent_framework/agent/coordinator.py:64,247-265` | 已实现 |
| 9 | 权限优先级固定: Capability -> Scoped -> hook | `:1697` | `agent_framework/agent/coordinator.py:212-220`; `agent_framework/agent/loop.py:157-171`; `agent_framework/tools/executor.py:117-124` | 已实现 |
| 10 | Skill override 仅作用当前 run 的 effective config | `:1698` | `agent_framework/agent/coordinator.py:150-175` | 已实现 |
| 11 | 子 Agent 默认不继承父 Skill override | `:1699` | `agent_framework/subagent/factory.py:49-58`（仅显式 override 时应用） | 已实现 |
| 12 | `spawn_agent` 返回给 LLM 的是摘要结果 | `:1700` | `agent_framework/tools/executor.py:199-203`（`summarize_result(...).model_dump()`） | 已实现 |
| 13 | `SHARED_WRITE` 只能通过父 `MemoryManager.remember()` 写回 | `:1701` | `agent_framework/subagent/memory_scope.py:70-93,121-145` | 已实现 |
| 14 | 默认记忆风格接近 GPT Saved Memories | `:1702` | `agent_framework/memory/default_manager.py:39-46,122-164` | 已实现 |
| 15 | 用户可列出/删除/启停/钉住 Saved Memories | `:1703` | `agent_framework/entry.py:231-272`; `agent_framework/memory/base_manager.py:136-175` | 已实现 |

---

## C. 关键流程条款补充对照（10/13/16/20章）

| 条款 | 文档行号 | 对应代码 | 状态 | 说明 |
|---|---:|---|---|---|
| 10.4 权限边界优先级 | `架构开发文档_opt.md:864-876` | `agent_framework/agent/coordinator.py:212-220`; `agent_framework/agent/loop.py:157-171` | 已实现 | 上界过滤 + 运行时二次拦截 |
| 13.5 Skill override 边界 | `:1280-1289` | `agent_framework/agent/coordinator.py:150-175` | 已实现 | override 只进 `EffectiveRunConfig` |
| 16.1 单 Agent 数据流 | `:1462-1489` | `agent_framework/agent/coordinator.py:62-140` | 已实现 | begin/loop/record/end 完整覆盖 |
| 16.2 子 Agent 派生流 | `:1492-1510` | `agent_framework/tools/executor.py:183-203`; `agent_framework/subagent/runtime.py:44-95`; `agent_framework/subagent/scheduler.py:84-140` | **部分实现** | `build spawn seed` 步骤未在主链路调用 |
| 20.1 子 Agent 默认禁 system/network | `:1659-1664` | `agent_framework/subagent/factory.py:70-86` | 已实现 | 默认阻断 `system/network/subagent` |
| 20.2 子 Agent 递归防护 | `:1665-1669` | `agent_framework/subagent/factory.py:59-61,113-114`; `tests/test_integration.py:764-784` | 已实现 | 强制 `allow_spawn_children=False` |
| 20.4 资源治理（配额/并发/超时） | `:1678-1684` | `agent_framework/subagent/scheduler.py:40-50,87-90,109,183-199` | 已实现 | quota + semaphore + timeout |

---

## D. 回归测试覆盖映射（是否“Claude 都实现”）

| 验证点 | 测试位置 | 结果 |
|---|---|---|
| Capability policy 对 schema 过滤 | `tests/test_integration.py:637-680` | 已通过 |
| Capability policy 运行时阻断 | `tests/test_integration.py:682-724` | 已通过 |
| A2A task_input 映射 | `tests/test_integration.py:726-763` | 已通过 |
| 子 Agent scoped executor 与递归防护 | `tests/test_integration.py:764-784` | 已通过 |
| setup_a2a 接线 delegation adapter | `tests/test_integration.py:787-810` | 已通过 |
| MCP sync server 过滤 | `tests/test_integration.py:812-826` | 已通过 |

补充:
- 你提供的两次测试记录显示: 首次失败点为 `configure_logging` 签名不匹配；二次已通过。
- 对应修复后状态可在 `agent_framework/entry.py:60` 与 `agent_framework/infra/logger.py:92-100` 对齐验证。

---

## E. 最终判定

- 是否“Claude 都实现”: **基本已实现**。
- 当前仍建议列为待完善项:
  - `16.2` 子流程中的 `build spawn seed` 尚未在 `SubAgentRuntime.spawn` 主路径接线调用（`ContextEngineer.build_spawn_seed` 已存在：`agent_framework/context/engineer.py:120-127`）。

